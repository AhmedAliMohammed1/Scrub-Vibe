# Deployment

Vercel production deployment targets `main`; pull requests receive preview deployments. Configure variables from `.env.example` in Vercel rather than committing credentials. No live deployment has been performed in the current checkpoint.
