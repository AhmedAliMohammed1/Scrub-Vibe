# Payments

Checkout will depend on a `PaymentProvider` contract with Paymob and cash-on-delivery implementations. Redirects are informational only. Authenticated, idempotent provider verification controls payment status. Card numbers and CVVs never enter this application.
