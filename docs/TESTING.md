# Testing

Run `pnpm lint`, `pnpm typecheck`, `pnpm test` and `pnpm build` for the foundation. Later phases add Supabase-backed integration tests and Playwright journeys for guest checkout, customer account and admin workflows. Security tests are release gates.
